# 问卷调查系统 - 项目结构与功能说明文档

> 📚 本文档详细介绍了问卷调查系统的完整项目结构、每个页面的功能、核心业务逻辑以及数据流转过程。
>
> **阅读对象**: 项目新成员、维护人员、或任何想要了解系统架构的开发者

---

## 📋 目录

1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [项目结构总览](#项目结构总览)
4. [核心目录详解](#核心目录详解)
5. [页面功能说明](#页面功能说明)
6. [核心业务逻辑](#核心业务逻辑)
7. [数据流转](#数据流转)
8. [开发指南](#开发指南)

---

## 项目概述

这是一个**前后端分离**的在线问卷调查系统，支持用户创建、发布、填写问卷，以及查看统计数据。系统分为**前台用户端**和**后台管理端**两个部分。

### 主要功能

- ✅ **用户功能**: 注册登录、创建问卷、填写问卷、查看结果、收藏问卷
- ✅ **管理员功能**: 问卷审核、用户管理、数据统计、公告管理
- ✅ **问卷功能**: 多种题型（单选、多选、文本、评分）、跳转逻辑、模板系统
- ✅ **积分系统**: 答题获得积分、积分排行榜
- ✅ **评论系统**: 问卷评价、评分、评论管理
- ✅ **数据统计**: 问卷数据可视化、选项统计、参与度分析

---

## 技术栈

### 前端框架

- **Vue 3** - 渐进式 JavaScript 框架（使用 Composition API）
- **Vite** - 下一代前端构建工具
- **Vue Router** - 官方路由管理器
- **Pinia** - Vue 3 状态管理库

### UI 组件库

- **Element Plus** - 基于 Vue 3 的组件库
- **ECharts** - 数据可视化图表库（用于统计页面）

### 后端 API

- **JSON Server** - 模拟 REST API（开发环境）
- **Axios** - HTTP 请求库

### 开发工具

- **ESLint** - 代码检查工具
- **Sass** - CSS 预处理器

---

## 项目结构总览

```
client/                          # 前端项目根目录
├── public/                      # 静态资源目录
├── src/                         # 源代码目录
│   ├── api/                     # API 接口层
│   ├── assets/                  # 静态资源（样式、图片）
│   ├── components/              # 公共组件
│   ├── composables/             # 组合式函数（业务逻辑复用）
│   ├── hooks/                   # 自定义 Hooks
│   ├── layouts/                 # 布局组件
│   ├── router/                  # 路由配置
│   ├── store/                   # 状态管理
│   ├── utils/                   # 工具函数
│   ├── views/                   # 页面组件
│   ├── App.vue                  # 根组件
│   └── main.js                  # 入口文件
├── docs/                        # 项目文档
├── db.json                      # 模拟数据库
├── index.html                   # HTML 模板
├── package.json                 # 项目依赖配置
└── vite.config.js              # Vite 配置文件
```

---

## 核心目录详解

### 📁 `/src/api/` - API 接口层

**作用**: 封装所有与后端交互的 API 请求

| 文件               | 说明           | 主要功能                              |
| ------------------ | -------------- | ------------------------------------- |
| `index.js`         | API 客户端配置 | 配置 Axios、请求/响应拦截器、错误处理 |
| `auth.js`          | 认证相关接口   | 登录、注册、登出、密码重置            |
| `user.js`          | 用户相关接口   | 用户信息、收藏、积分、答题记录        |
| `survey.js`        | 问卷相关接口   | 问卷列表、详情、提交答案、评论        |
| `questionnaire.js` | 问卷创建接口   | 创建问卷、保存草稿、发布问卷          |
| `admin.js`         | 管理员接口     | 用户管理、问卷审核、数据统计          |

**核心代码示例**:

```javascript
// api/index.js - API 客户端配置
import axios from "axios";

const apiClient = axios.create({
  baseURL: "http://localhost:3002", // API 基础地址
  timeout: 10000, // 请求超时时间
});

// 请求拦截器：添加 token
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem("token");
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 响应拦截器：统一错误处理
apiClient.interceptors.response.use(
  (response) => response.data,
  (error) => {
    if (error.response?.status === 401) {
      // 未授权，跳转登录
      router.push("/login");
    }
    return Promise.reject(error);
  }
);
```

---

### 📁 `/src/components/` - 公共组件

**作用**: 存放可复用的 UI 组件

| 组件                      | 说明           | 使用场景                          |
| ------------------------- | -------------- | --------------------------------- |
| `QuestionnaireEditor.vue` | 统一问卷编辑器 | 创建/编辑问卷（前台+后台共用）    |
| `CommentsDialog.vue`      | 评论管理对话框 | 查看/管理问卷评论                 |
| `StatsDialog.vue`         | 统计数据弹窗   | 展示问卷统计数据（前台+后台共用） |
| `SurveyCardEnhanced.vue`  | 问卷卡片组件   | 问卷列表展示                      |
| `DynamicProgress.vue`     | 动态进度条     | 答题进度展示                      |
| `TreeGrowth.vue`          | 树木成长动画   | 积分系统可视化                    |
| `LiquidFill.vue`          | 水波球图表     | 完成度可视化                      |

**重点组件说明**:

#### QuestionnaireEditor.vue - 问卷编辑器

- **功能**: 创建和编辑问卷
- **支持题型**: 单选题、多选题、文本题、评分题
- **核心特性**:
  - 拖拽排序问题
  - 跳转逻辑设置（选项可跳转到指定题目或结束）
  - 必填项设置
  - 自动保存草稿（每 30 秒）
  - 管理员/用户模式切换（通过 `isAdmin` 判断）

---

### 📁 `/src/composables/` - 组合式函数

**作用**: 提取可复用的业务逻辑（类似 Vue 2 的 mixins，但更灵活）

| 文件                        | 说明           | 主要功能                     |
| --------------------------- | -------------- | ---------------------------- |
| `useQuestionnaireEditor.js` | 问卷编辑器逻辑 | 题目管理、选项管理、保存发布 |
| `useQuestionnaireLogic.js`  | 答题逻辑       | 跳转控制、进度计算、验证     |
| `useAnswerTimer.js`         | 答题计时器     | 记录答题时长                 |
| `useHomeLogic.js`           | 首页逻辑       | 问卷筛选、搜索、分页         |
| `useTemplates.js`           | 模板管理       | 模板加载、应用模板           |

**核心逻辑说明**:

#### useQuestionnaireLogic.js - 答题跳转逻辑

```javascript
/**
 * 核心功能：处理答题流程中的跳转逻辑
 *
 * 关键方法：
 * 1. nextQuestion() - 判断下一题
 *    - 检查当前题目是否设置了跳转规则
 *    - 如果选项有 isEnd=true，直接结束问卷
 *    - 如果有 targetQuestion，跳转到指定题目
 *    - 否则跳转到下一题
 *
 * 2. computeExpectedPath() - 计算预期路径
 *    - 根据用户已回答的问题，计算应该经过的题目路径
 *    - 用于计算真实进度（不是简单的 当前题号/总题数）
 *
 * 3. validateRequiredQuestions() - 验证必填项
 *    - 只验证 expectedPath 中的必填题
 *    - 避免跳过的题目被错误验证
 */
```

---

### 📁 `/src/hooks/` - 自定义 Hooks

| 文件               | 说明          | 使用场景             |
| ------------------ | ------------- | -------------------- |
| `useListFilter.js` | 列表过滤 Hook | 搜索、筛选、分页功能 |

**useListFilter.js 使用示例**:

```javascript
// 在任何需要列表功能的页面使用
const {
  searchKeyword, // 搜索关键词（双向绑定）
  currentPage, // 当前页码
  pageSize, // 每页数量
  filteredList, // 过滤后的列表
  filteredTotal, // 过滤后总数
  handleSearch, // 搜索处理函数
  handleFilter, // 筛选处理函数
  handlePageChange, // 翻页处理函数
} = useListFilter({
  sourceList: surveys, // 原始数据
  searchFields: ["title", "desc"], // 搜索字段
  sortFn: (a, b) => b.id - a.id, // 排序函数
});
```

---

### 📁 `/src/layouts/` - 布局组件

| 文件              | 说明       | 使用对象 |
| ----------------- | ---------- | -------- |
| `UserLayout.vue`  | 用户端布局 | 普通用户 |
| `AdminLayout.vue` | 管理员布局 | 管理员   |

**布局特点**:

- **UserLayout**: 顶部导航 + 侧边栏 + 主内容区
- **AdminLayout**: 左侧菜单 + 顶部面包屑 + 主内容区

---

### 📁 `/src/router/` - 路由配置

| 文件             | 说明                 |
| ---------------- | -------------------- |
| `index.js`       | 路由总配置、全局守卫 |
| `userRoutes.js`  | 用户端路由           |
| `adminRoutes.js` | 管理员路由           |

**路由守卫逻辑**:

```javascript
// 全局前置守卫
router.beforeEach((to, from, next) => {
  const userStore = useUserStore();

  // 1. 检查是否需要登录
  if (to.meta.requiresAuth && !userStore.isLoggedIn) {
    next("/login");
    return;
  }

  // 2. 检查是否需要管理员权限
  if (to.meta.requiresAdmin && !userStore.isAdmin) {
    next("/403");
    return;
  }

  next();
});
```

---

### 📁 `/src/store/` - 状态管理

| 文件               | 说明       | 存储内容                 |
| ------------------ | ---------- | ------------------------ |
| `index.js`         | Pinia 实例 | 创建 Pinia               |
| `user.js`          | 用户状态   | 用户信息、登录状态、权限 |
| `questionnaire.js` | 问卷状态   | 当前问卷数据、草稿       |
| `data.js`          | 数据状态   | 分类、标签等基础数据     |

**user.js 核心状态**:

```javascript
export const useUserStore = defineStore("user", {
  state: () => ({
    token: localStorage.getItem("token") || null,
    profile: JSON.parse(localStorage.getItem("userProfile") || "null"),
    isLoggedIn: false,
    isAdmin: false,
  }),

  actions: {
    async login(credentials) {
      // 登录逻辑
      const response = await loginApi(credentials);
      this.token = response.token;
      this.profile = response.user;
      this.isLoggedIn = true;
      this.isAdmin = response.user.role === "admin";
      localStorage.setItem("token", response.token);
      localStorage.setItem("userProfile", JSON.stringify(response.user));
    },

    logout() {
      // 登出逻辑
      this.token = null;
      this.profile = null;
      this.isLoggedIn = false;
      this.isAdmin = false;
      localStorage.removeItem("token");
      localStorage.removeItem("userProfile");
    },
  },
});
```

---

### 📁 `/src/utils/` - 工具函数

| 文件                | 说明     | 主要功能               |
| ------------------- | -------- | ---------------------- |
| `logicEngine.js`    | 逻辑引擎 | 处理问卷跳转逻辑       |
| `scoring.js`        | 评分计算 | 计算问卷得分、结果分析 |
| `recommendation.js` | 推荐算法 | 问卷推荐、相似度计算   |

---

## 页面功能说明

### 🏠 前台页面（/views/frontend/）

#### 1. 首页模块（/home/）

##### HomePage.vue - 首页

**功能**: 问卷展示、搜索、筛选

- **核心逻辑**:
  ```
  1. 加载问卷列表（调用 listSurveys API）
  2. 提供搜索框（按标题搜索）
  3. 分类筛选（按问卷分类）
  4. 排序功能（最新、最热、评分）
  5. 分页展示
  ```
- **数据流**:
  ```
  API (listSurveys)
    → Store (可选)
    → Component State (surveys)
    → useListFilter Hook (过滤分页)
    → UI 渲染
  ```

**子组件**:

- `FeaturedSurveys.vue` - 精选问卷轮播
- `SearchFilter.vue` - 搜索筛选栏
- `StatsCards.vue` - 统计卡片（总问卷数、参与人数等）
- `SurveyCard.vue` - 问卷卡片
- `SurveyList.vue` - 问卷列表

---

#### 2. 认证模块（/auth/）

##### LoginPage.vue - 登录页

**功能**: 用户登录

- **表单字段**: 用户名、密码、记住我
- **验证规则**:
  - 用户名：必填，3-20 字符
  - 密码：必填，6-20 字符
- **登录流程**:
  ```javascript
  1. 用户输入账号密码
  2. 前端表单验证
  3. 调用 loginApi(credentials)
  4. 后端返回 token + 用户信息
  5. 存储到 Store 和 localStorage
  6. 跳转到首页或 redirect 参数指定的页面
  ```

##### RegisterPage.vue - 注册页

**功能**: 用户注册

- **表单字段**: 用户名、邮箱、密码、确认密码
- **验证规则**:
  - 用户名：唯一、3-20 字符
  - 邮箱：格式验证、唯一
  - 密码：6-20 字符、需包含字母和数字
  - 确认密码：与密码一致

##### ResetPasswordPage.vue - 重置密码

**功能**: 忘记密码找回

- **流程**: 邮箱验证 → 发送验证码 → 重置密码

---

#### 3. 问卷模块（/questionnaire/）

##### TemplateSelectionPage.vue - 模板选择页

**功能**: 选择问卷模板创建

- **模板来源**:
  - 系统预置模板
  - 管理员设置的模板问卷（`isTemplate=true`）
- **操作**:
  - 浏览模板
  - 预览模板
  - 使用模板创建（复制模板数据，生成新问卷）

##### CreatePage.vue - 创建问卷页

**功能**: 从空白创建问卷

- **使用组件**: `QuestionnaireEditor.vue`
- **操作流程**:
  ```
  1. 填写问卷基本信息（标题、描述、分类）
  2. 添加问题（支持多种题型）
  3. 设置跳转逻辑（可选）
  4. 保存为草稿或直接发布
  ```

**子组件**:

- `TemplateCard.vue` - 模板卡片
- `TemplatePreviewDialog.vue` - 模板预览对话框

---

#### 4. 问卷填写模块（/survey/）

##### DetailPage.vue - 问卷详情页

**功能**: 查看问卷详细信息

- **展示内容**:
  - 问卷基本信息（标题、描述、作者）
  - 统计数据（参与人数、平均评分、预计用时）
  - 问题预览（前 3 题）
  - 用户评论
  - 相关推荐（同分类问卷）
- **核心逻辑**:
  ```javascript
  // 加载推荐问卷
  const loadRecommendedSurveys = async (category, currentId) => {
    // 1. 获取所有问卷
    const allSurveys = await listSurveys();

    // 2. 筛选相同分类且已发布的问卷
    let recommended = allSurveys
      .filter(
        (s) =>
          s.category === category &&
          s.id !== currentId &&
          s.status === "published"
      )
      .sort((a, b) => (b.participantCount || 0) - (a.participantCount || 0))
      .slice(0, 3);

    // 3. 不足 3 个则用其他热门问卷补充
    if (recommended.length < 3) {
      const others = allSurveys
        .filter((s) => s.id !== currentId && s.status === "published")
        .sort((a, b) => (b.participantCount || 0) - (a.participantCount || 0))
        .slice(0, 3 - recommended.length);
      recommended = [...recommended, ...others];
    }

    return recommended;
  };
  ```

##### AnswerPage.vue - 答题页

**功能**: 填写问卷

- **核心特性**:
  - **题目类型处理**:
    - 单选题：Radio 单选
    - 多选题：Checkbox 多选
    - 文本题：Textarea 文本框
    - 评分题：Rate 评分组件
  - **跳转逻辑**:
    ```javascript
    // 点击"下一题"时
    const nextQuestion = () => {
      const currentQ = questions[currentIndex];
      const userAnswer = answers[currentQ.id];

      // 1. 检查是否设置了跳转规则
      if (currentQ.jumpRules && userAnswer) {
        const selectedOption = currentQ.options.find(
          (opt) => opt.id === userAnswer
        );

        // 2. 如果选项标记为"结束问卷"
        if (selectedOption?.isEnd) {
          return "complete"; // 直接跳转到结果页
        }

        // 3. 如果有指定跳转目标
        if (selectedOption?.targetQuestion) {
          const targetIndex = questions.findIndex(
            (q) => q.id === selectedOption.targetQuestion
          );
          currentIndex = targetIndex;
          return;
        }
      }

      // 4. 默认跳转下一题
      currentIndex++;
    };
    ```
  - **进度计算**:
    ```javascript
    // 不是简单的 当前题号/总题数
    // 而是根据用户的答题路径动态计算
    const computeProgress = () => {
      const expectedPath = computeExpectedPath(); // 计算预期经过的题目
      const answeredInPath = expectedPath.filter((qId) => answers[qId]).length;
      return (answeredInPath / expectedPath.length) * 100;
    };
    ```
  - **答题计时**: 使用 `useAnswerTimer` 记录答题时长

**子组件**:

- `AnimatedQuestion.vue` - 问题动画组件
- `ProgressCard.vue` - 进度卡片
- `RandomProgress.vue` - 随机进度动画

##### ResultPage.vue - 结果页

**功能**: 展示答题结果

- **展示内容**:
  - 答题得分
  - 结果分析
  - 答题时长
  - 评分和评论功能
  - 分享功能
- **积分奖励逻辑**:
  ```javascript
  // 首次完成问卷奖励积分
  const awardPoints = async () => {
    const basePoints = 10; // 基础积分
    const timeBonus = calculateTimeBonus(duration); // 时长奖励
    const qualityBonus = calculateQualityBonus(answers); // 答题质量奖励

    const totalPoints = basePoints + timeBonus + qualityBonus;
    await addUserPoints(userId, totalPoints);
  };
  ```

---

#### 5. 个人中心模块（/user/）

##### ProfileLayout.vue - 个人中心布局

**功能**: 个人中心主布局

- **左侧菜单**:
  - 个人信息
  - 我的问卷（已创建、已发布、待审核、草稿、回收站）
  - 答题记录
  - 我的收藏
  - 我的成就
  - 设置

**子页面**:

###### InfoPage.vue - 个人信息

- 查看/编辑个人资料
- 修改头像
- 修改密码
- 账户统计

###### CollectionsPage.vue - 我的收藏

- **功能**: 管理收藏的问卷
- **特性**:
  - 搜索、筛选、排序
  - 批量取消收藏
  - 快速开始答题
- **数据加载**:
  ```javascript
  const loadCollections = async () => {
    const userId = userStore.profile?.id;
    const favorites = await getFavoritesApi(userId); // 获取收藏列表
    collections.value = favorites;
  };
  ```

###### AnsweredPage.vue - 答题记录

- 查看已完成的问卷
- 查看答题详情
- 重新答题

###### CreatedPage.vue - 已创建问卷

- 查看自己创建的所有问卷
- 统计数据概览

###### PublishedPage.vue - 已发布问卷

- 查看已发布的问卷
- **关键功能**:
  - 查看数据统计（使用 `StatsDialog` 组件）
  - 分享问卷
  - 查看评论
  - 下架问卷
  - 编辑问卷（需重新审核）
- **统计数据展示**:
  ```javascript
  const viewResults = async (id) => {
    // 调用 getSurveyDetail 获取完整数据
    // 数据中已包含每个选项的 selectedCount（选择次数）
    const surveyData = await getSurveyDetail(id);

    // 使用 StatsDialog 展示统计
    currentSurveyStats.value = surveyData;
    statsDialogVisible.value = true;
  };
  ```

###### PendingPage.vue - 待审核问卷

- 查看待管理员审核的问卷
- 撤回审核

###### TrashPage.vue - 回收站

- 查看已删除的问卷
- 恢复问卷
- 彻底删除

###### AchievementsPage.vue - 我的成就

- 查看获得的成就徽章
- 成就进度

---

#### 6. 排行榜模块（/rankings/）

##### RankingsLayout.vue - 排行榜布局

**子页面**:

###### UserPointsPage.vue - 积分排行

- 展示用户积分排行榜
- 显示当前用户排名

###### ParticipationPage.vue - 参与度排行

- 展示问卷参与人数排行

###### RatingPage.vue - 评分排行

- 展示问卷评分排行

---

### 🔧 后台页面（/views/backend/admin/）

#### DashboardPage.vue - 管理员仪表盘

**功能**: 数据概览、快捷操作

- **展示数据**:
  - 用户总数、问卷总数、今日新增
  - 待审核问卷数量
  - 数据趋势图表
  - 最近活动日志

---

#### 用户管理（/components/users/）

##### UserManagePage.vue - 用户管理

- **功能**:
  - 用户列表（搜索、筛选、分页）
  - 查看用户详情
  - 禁用/启用用户
  - 修改用户积分
  - 删除用户

##### AdminManagePage.vue - 管理员管理

- 管理员列表
- 添加/删除管理员

##### CreateUserPage.vue - 创建用户

- 批量创建用户
- 导入用户数据

---

#### 问卷管理（/components/questionnaires/）

##### QuestionnaireListPage.vue - 问卷列表

- **功能**:
  - 查看所有问卷（包括用户创建的）
  - 搜索、筛选（状态、分类）
  - **统计数据**: 点击"统计"按钮，使用 `StatsDialog` 展示
  - **评论管理**: 点击"评论"按钮，使用 `CommentsDialog` 管理评论
  - 上架/下架问卷
  - 设为模板/取消模板
  - 复制问卷
  - 删除问卷
- **核心逻辑**:

  ```javascript
  // 查看统计（与前台共用 StatsDialog）
  const viewStatistics = async (id) => {
    const surveyData = await getSurveyDetail(id); // 获取完整数据
    currentStats.value = surveyData;
    statisticsDialogVisible.value = true;
  };

  // 设为模板
  const setTemplate = async (id) => {
    await apiClient.patch(`/surveys/${id}`, { isTemplate: true });
    await recordAdminActivity({
      // 记录操作日志
      title: "设置模板",
      description: `将问卷"${survey.title}"设置为模板`,
    });
  };
  ```

##### PendingQuestionnairesPage.vue - 待审核问卷

- **功能**:
  - 查看待审核问卷列表
  - 预览问卷内容
  - 审核操作（通过/拒绝）
  - 审核意见
- **审核流程**:
  ```javascript
  const approve = async (id) => {
    // 1. 更新问卷状态为 published
    await updateSurveyStatusApi(id, "published");

    // 2. 通知用户（可选）
    await sendNotification(survey.creatorId, "您的问卷已通过审核");

    // 3. 记录审核日志
    await recordAdminActivity({
      title: "审核通过",
      description: `问卷"${survey.title}"审核通过`,
    });
  };
  ```

---

#### 公告管理（/components/announcements/）

##### AnnouncementManagePage.vue - 公告管理

- 创建/编辑/删除公告
- 置顶公告
- 发布/下架公告

---

#### 个人资料（/components/profile/）

##### AdminProfilePage.vue - 管理员个人资料

- 查看/编辑管理员信息
- 修改密码

---

### 🚨 错误页面（/views/error/）

##### 403.vue - 无权限页面

- 提示用户没有访问权限

##### 404.vue - 页面不存在

- 提示页面未找到

---

## 核心业务逻辑

### 1. 问卷跳转逻辑系统

**实现文件**: `composables/useQuestionnaireLogic.js`

**核心概念**:

- **跳转规则**: 每个选项可设置跳转目标
- **结束标记**: 选项可标记为"结束问卷"
- **预期路径**: 根据用户答案动态计算应该经过的题目

**关键方法**:

```javascript
// 1. 计算下一题
const nextQuestion = () => {
  const currentQuestion = questions[currentQuestionIndex];
  const userAnswer = answers[currentQuestion.id];

  // 检查跳转规则
  if (currentQuestion.jumpRules && userAnswer) {
    const selectedOption = currentQuestion.options.find(
      (opt) => opt.id === userAnswer
    );

    // 情况 A: 结束问卷
    if (selectedOption?.isEnd) {
      return "complete";
    }

    // 情况 B: 跳转到指定题目
    if (selectedOption?.targetQuestion) {
      const targetIndex = questions.findIndex(
        (q) => q.id === selectedOption.targetQuestion
      );
      currentQuestionIndex = targetIndex;
      return;
    }
  }

  // 情况 C: 默认下一题
  currentQuestionIndex++;
};

// 2. 计算预期路径（用于进度和验证）
const computeExpectedPath = () => {
  const path = new Set();
  let currentIndex = 0;

  while (currentIndex < questions.length) {
    const question = questions[currentIndex];
    path.add(question.id);

    // 根据答案决定下一题
    const answer = answers[question.id];
    if (answer && question.jumpRules) {
      const option = question.options.find((opt) => opt.id === answer);

      if (option?.isEnd) break; // 遇到结束标记，停止

      if (option?.targetQuestion) {
        currentIndex = questions.findIndex(
          (q) => q.id === option.targetQuestion
        );
        continue;
      }
    }

    currentIndex++;
  }

  return Array.from(path);
};

// 3. 验证必填项（只验证预期路径上的题目）
const validateRequiredQuestions = () => {
  const expectedPath = computeExpectedPath();
  const expectedPathSet = new Set(expectedPath);

  const unansweredRequired = questions
    .filter((q) => q.required && expectedPathSet.has(q.id))
    .filter((q) => !answers[q.id]);

  if (unansweredRequired.length > 0) {
    throw new Error(`还有 ${unansweredRequired.length} 道必答题未回答`);
  }
};
```

**使用示例**:

```javascript
// 在 AnswerPage.vue 中
import { useQuestionnaireLogic } from "@/composables/useQuestionnaireLogic";

const {
  currentQuestionIndex,
  answers,
  progress,
  nextQuestion,
  previousQuestion,
  submitAnswers,
} = useQuestionnaireLogic(survey);

// 点击下一题
const handleNext = () => {
  const result = nextQuestion();
  if (result === "complete") {
    // 跳转到结果页
    router.push(`/surveys/result/${surveyId}`);
  }
};
```

---

### 2. 问卷编辑器逻辑

**实现文件**: `composables/useQuestionnaireEditor.js`

**核心功能**:

```javascript
// 1. 初始化编辑器
const initialize = async (mode, id) => {
  if (mode === "edit" && id) {
    // 编辑模式：加载现有问卷
    const survey = await getSurveyDetailApi(id);
    questionnaireData.value = survey;
    questions.value = survey.questionList || [];
  } else {
    // 创建模式：新建问卷
    resetQuestionnaire();
  }
};

// 2. 添加问题
const handleAddQuestion = (type) => {
  const newQuestion = {
    id: Date.now(),
    type,
    title: "",
    description: "",
    required: false,
    options:
      type === "single" || type === "multiple"
        ? [{ id: 1, text: "选项 1" }]
        : [],
    jumpRules: null, // 跳转规则
  };

  questions.value.push(newQuestion);
};

// 3. 设置跳转规则
const setJumpRule = (questionId, optionId, rule) => {
  const question = questions.value.find((q) => q.id === questionId);
  if (!question.jumpRules) {
    question.jumpRules = {};
  }

  // rule 格式: { isEnd: boolean, targetQuestion: number }
  question.jumpRules[optionId] = rule;

  // 同步到 option 对象（方便读取）
  const option = question.options.find((opt) => opt.id === optionId);
  if (option) {
    option.isEnd = rule.isEnd;
    option.targetQuestion = rule.targetQuestion;
  }
};

// 4. 保存草稿（自动保存）
const saveDraft = async () => {
  const draftData = buildQuestionnaireData();
  draftData.status = "draft";

  if (questionnaireData.value.id) {
    // 更新现有草稿
    await apiClient.patch(`/surveys/${questionnaireData.value.id}`, draftData);
  } else {
    // 创建新草稿
    const result = await apiClient.post("/surveys", draftData);
    questionnaireData.value.id = result.id;
  }

  lastSavedAt.value = new Date();
};

// 5. 发布问卷
const publishQuestionnaire = async () => {
  // 验证数据
  validateQuestionnaireData();

  const data = buildQuestionnaireData();
  data.status = userStore.isAdmin ? "published" : "pending"; // 管理员直接发布

  if (questionnaireData.value.id) {
    await apiClient.patch(`/surveys/${questionnaireData.value.id}`, data);
  } else {
    await apiClient.post("/surveys", data);
  }

  ElMessage.success(userStore.isAdmin ? "发布成功" : "提交审核成功");
};

// 6. 自动保存定时器
let autoSaveTimer = null;
const startAutoSave = () => {
  autoSaveTimer = setInterval(() => {
    if (hasUnsavedChanges()) {
      saveDraft();
    }
  }, 30000); // 每 30 秒保存一次
};
```

---

### 3. 积分系统

**积分获取规则**:

| 行为     | 基础积分 | 奖励规则                      |
| -------- | -------- | ----------------------------- |
| 完成问卷 | 10 分    | 首次完成 +5 分                |
| 创建问卷 | 20 分    | 问卷被收藏 +2 分/次           |
| 每日签到 | 5 分     | 连续签到 +1 分/天（最高 +10） |
| 评价问卷 | 2 分     | 优质评价（>50 字）+3 分       |

**实现逻辑**:

```javascript
// api/user.js
export const addUserPoints = async (userId, points, reason) => {
  // 1. 获取用户当前积分
  const user = await apiClient.get(`/users/${userId}`);

  // 2. 更新积分
  const newPoints = (user.points || 0) + points;
  await apiClient.patch(`/users/${userId}`, { points: newPoints });

  // 3. 记录积分历史
  await apiClient.post("/pointsHistory", {
    userId,
    points,
    reason,
    timestamp: new Date().toISOString(),
  });

  return newPoints;
};

// 使用示例
await addUserPoints(userId, 10, "完成问卷");
```

---

### 4. 评论与评分系统

**评论数据结构**:

```javascript
{
  id: "comment_123",
  surveyId: 456,          // 问卷 ID
  userId: 789,            // 用户 ID
  username: "张三",       // 用户名
  avatar: "url",          // 头像
  rating: 4.5,            // 评分（1-5）
  content: "很好的问卷", // 评论内容
  createdAt: "2024-01-01T12:00:00Z",
  isDeleted: false        // 软删除标记
}
```

**实现逻辑**:

```javascript
// 提交评论
export const createCommentApi = async (data) => {
  // 1. 创建评论
  const comment = await apiClient.post("/comments", {
    ...data,
    createdAt: new Date().toISOString(),
  });

  // 2. 更新问卷平均评分
  const survey = await apiClient.get(`/surveys/${data.surveyId}`);
  const comments = await apiClient.get(`/comments?surveyId=${data.surveyId}`);

  const avgRating =
    comments.reduce((sum, c) => sum + c.rating, 0) / comments.length;
  await apiClient.patch(`/surveys/${data.surveyId}`, {
    averageRating: avgRating,
    ratingCount: comments.length,
  });

  // 3. 奖励积分
  await addUserPoints(data.userId, 2, "发表评论");

  return comment;
};
```

---

## 数据流转

### 完整答题流程

```
1. 用户访问问卷详情页
   DetailPage.vue
   ↓
   调用 getSurveyDetail(id)
   ↓
   展示问卷信息、评论、推荐

2. 点击"开始测试"
   ↓
   检查登录状态
   ↓
   跳转到 AnswerPage.vue

3. 开始答题
   AnswerPage.vue 加载
   ↓
   调用 getSurveyDetail(id) 获取题目
   ↓
   初始化 useQuestionnaireLogic
   ↓
   启动 useAnswerTimer 计时

4. 用户答题过程
   ↓
   用户选择答案 → answers[questionId] = answer
   ↓
   点击"下一题" → nextQuestion()
   ↓
   检查跳转规则:
     - 有 isEnd? → 直接跳转结果页
     - 有 targetQuestion? → 跳转到指定题
     - 否则 → 下一题
   ↓
   更新 progress（根据 expectedPath 计算）

5. 提交问卷
   ↓
   validateRequiredQuestions() 验证
   ↓
   调用 submitSurveyApi({
     userId,
     surveyId,
     answers,
     duration
   })
   ↓
   后端处理:
     - 创建答案记录（/answers）
     - 更新问卷统计（participantCount++）
     - 更新选项统计（selectedCount++）
     - 计算得分
   ↓
   奖励积分
   ↓
   跳转到 ResultPage.vue

6. 查看结果
   ResultPage.vue
   ↓
   展示得分、分析、评论功能
   ↓
   用户可以:
     - 评分评论
     - 分享问卷
     - 查看排行榜
```

### 问卷创建流程

```
1. 用户访问创建页面
   选择空白创建 / 从模板创建
   ↓
   加载 QuestionnaireEditor.vue

2. 填写问卷信息
   ↓
   基本信息: 标题、描述、分类
   ↓
   添加问题: 调用 handleAddQuestion(type)
   ↓
   配置选项: 添加/删除选项
   ↓
   设置跳转: setJumpRule(questionId, optionId, rule)

3. 自动保存
   ↓
   每 30 秒调用 saveDraft()
   ↓
   保存到 localStorage（本地备份）
   ↓
   保存到服务器（status='draft'）

4. 发布问卷
   ↓
   点击"发布"按钮
   ↓
   validateQuestionnaireData() 验证
   ↓
   调用 publishQuestionnaire()
   ↓
   判断用户角色:
     - 管理员 → status='published' (直接发布)
     - 普通用户 → status='pending' (提交审核)
   ↓
   保存到服务器
   ↓
   跳转到对应列表页

5. 审核流程（仅普通用户）
   ↓
   管理员在 PendingQuestionnairesPage.vue 查看
   ↓
   审核操作:
     - 通过 → status='published'
     - 拒绝 → status='draft' + 添加审核意见
   ↓
   通知用户
```

### 统计数据更新流程

```
1. 用户提交问卷答案
   ↓
   调用 submitSurveyApi(data)

2. 更新选项统计
   ↓
   遍历用户的每个答案
   ↓
   对于单选/多选题:
     找到对应选项
     ↓
     selectedCount++

3. 保存答案记录
   ↓
   创建 /answers 记录:
   {
     userId,
     surveyId,
     answers: [
       { questionId, answer, text }
     ],
     submittedAt,
     duration
   }

4. 更新问卷统计
   ↓
   participantCount++（参与人数）
   ↓
   responseCount++（回答数）

5. 查看统计
   ↓
   前台: PublishedPage.vue → viewResults(id)
   后台: QuestionnaireListPage.vue → viewStatistics(id)
   ↓
   调用 getSurveyDetail(id)
   ↓
   返回数据已包含统计信息:
   {
     questionList: [
       {
         id, title, type,
         options: [
           { id, text, selectedCount }  // 已包含统计
         ]
       }
     ],
     answers: [...],  // 所有答案记录
     participantCount  // 参与人数
   }
   ↓
   StatsDialog.vue 渲染统计图表
```

---

## 开发指南

### 启动项目

```bash
# 1. 安装依赖
npm install

# 2. 启动 JSON Server（模拟后端）
npm run server
# 或
npx json-server --watch db.json --port 3002

# 3. 启动开发服务器
npm run dev

# 4. 访问应用
浏览器打开: http://localhost:5173
```

### 测试账号

```javascript
// 管理员账号
username: admin;
password: admin123;

// 普通用户账号
username: user01;
password: 123456;
```

### 新增页面步骤

1. **创建页面组件**

   ```bash
   # 在 views 对应目录下创建
   src/views/frontend/xxx/NewPage.vue
   ```

2. **配置路由**

   ```javascript
   // router/userRoutes.js 或 adminRoutes.js
   {
     path: '/xxx',
     component: NewPage,
     meta: {
       requiresAuth: true,  // 需要登录
       requiresAdmin: false // 不需要管理员权限
     }
   }
   ```

3. **添加 API 接口**

   ```javascript
   // api/xxx.js
   export const getDataApi = async () => {
     return await apiClient.get("/xxx");
   };
   ```

4. **使用状态管理（可选）**
   ```javascript
   // store/xxx.js
   export const useXxxStore = defineStore("xxx", {
     state: () => ({ data: [] }),
     actions: {
       async loadData() {
         this.data = await getDataApi();
       },
     },
   });
   ```

### 常用代码片段

#### 1. 使用 useListFilter Hook

```vue
<script setup>
import { useListFilter } from "@/hooks/useListFilter";

// 原始数据
const sourceList = ref([]);

// 使用 Hook
const {
  searchKeyword,
  currentPage,
  pageSize,
  filteredList,
  filteredTotal,
  handleSearch,
  handlePageChange,
} = useListFilter({
  sourceList,
  searchFields: ["title", "description"],
  sortFn: (a, b) => new Date(b.createdAt) - new Date(a.createdAt),
});
</script>

<template>
  <!-- 搜索框 -->
  <el-input
    v-model="searchKeyword"
    @input="handleSearch"
    placeholder="搜索..."
  />

  <!-- 列表 -->
  <div v-for="item in filteredList" :key="item.id">
    {{ item.title }}
  </div>

  <!-- 分页 -->
  <el-pagination
    v-model:current-page="currentPage"
    v-model:page-size="pageSize"
    :total="filteredTotal"
    @current-change="handlePageChange"
  />
</template>
```

#### 2. 调用 API

```javascript
import { getSurveysApi } from "@/api/survey";
import { ElMessage } from "element-plus";

const loadData = async () => {
  try {
    loading.value = true;
    const data = await getSurveysApi();
    surveys.value = data;
  } catch (error) {
    ElMessage.error("加载失败：" + error.message);
  } finally {
    loading.value = false;
  }
};
```

#### 3. 权限判断

```vue
<script setup>
import { useUserStore } from "@/store/user";

const userStore = useUserStore();
</script>

<template>
  <!-- 仅登录用户可见 -->
  <div v-if="userStore.isLoggedIn">欢迎，{{ userStore.profile.username }}</div>

  <!-- 仅管理员可见 -->
  <el-button v-if="userStore.isAdmin" @click="handleAdminAction">
    管理员操作
  </el-button>
</template>
```

---

## 常见问题

### 1. 跳转逻辑不生效？

**检查清单**:

- [ ] 选项是否设置了跳转规则？
- [ ] `isEnd` 或 `targetQuestion` 是否正确？
- [ ] `useQuestionnaireLogic` 是否正确初始化？
- [ ] 跳转目标题目 ID 是否存在？

### 2. 统计数据不准确？

**可能原因**:

- 提交答案时未更新 `selectedCount`
- `getSurveyDetail` 返回的数据未包含统计
- 答案数据格式不正确

**解决方法**:
查看 `api/survey.js` 中 `submitSurveyApi` 的实现，确保正确更新了选项统计。

### 3. 自动保存不工作？

**检查**:

- 是否调用了 `startAutoSave()`？
- 定时器是否被清除？
- `hasUnsavedChanges()` 逻辑是否正确？

---

## 项目文档索引

| 文档                            | 说明             |
| ------------------------------- | ---------------- |
| `积分系统实现总结.md`           | 积分系统详细设计 |
| `问卷创建与跳转逻辑开发文档.md` | 跳转逻辑实现指南 |
| `REFACTORING_SUMMARY.md`        | 代码重构总结     |
| `项目结构与功能说明.md`         | 本文档           |

---

## 总结

本文档详细介绍了问卷调查系统的：

- ✅ **完整项目结构** - 每个文件和文件夹的作用
- ✅ **所有页面功能** - 每个页面做什么、怎么实现
- ✅ **核心业务逻辑** - 跳转逻辑、编辑器、积分系统、评论系统
- ✅ **数据流转过程** - 从用户操作到数据存储的完整链路
- ✅ **开发指南** - 如何启动、如何添加新功能

**建议阅读顺序**:

1. 先了解项目概述和技术栈
2. 浏览项目结构，了解各目录作用
3. 重点阅读核心业务逻辑（特别是跳转逻辑）
4. 查看具体页面功能说明
5. 参考开发指南进行开发

如有疑问，可以：

- 查看对应的源代码文件
- 阅读其他专题文档
- 运行项目亲自体验功能

---

**文档版本**: v1.0  
**最后更新**: 2024-01-01  
**维护者**: 开发团队
